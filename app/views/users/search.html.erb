<%= stylesheet_link_tag 'posts', :media => 'screen' %>
<h2 align="center">Search results for : <i><%= @user_opt%></i> </h2>

<table>
  <tr>
    <th>Username</th>
    <th>E-mail</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.username %></td>
    <td><%= user.e_mail %></td>
    <td><%= link_to 'Show', user %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>
