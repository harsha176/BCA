<%= form_for(@reply) do |f| %>
  <% if @reply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reply.errors.count, "error") %> prohibited this reply from being saved:</h2>

      <ul>
      <% @reply.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="field">
    <%= f.label :Post %><br />
      <%= f.hidden_field :post_id, :value=> @reply.post_id%>
      <%= f.label Post.find(@reply.post_id).title%>
      <%= f.label Post.find(@reply.post_id).message%>
  </div>
  <div class="field">
    <%= f.label :reply %><br />
    <%= f.text_area :reply %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
